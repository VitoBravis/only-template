import Component, { ComponentProps } from '@/base/component';

export default class SpoilerSection extends Component {
    constructor(element: ComponentProps) {
        super(element);
    }

    destroy = () => {
        // Destroy functions
    }
}
