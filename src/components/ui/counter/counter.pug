mixin counter(props)
    .counter&attributes(attributes)
        .container
            button.counter__btn.--decrement
                span.counter__btn__icon -
            span.counter__count 0 
            button.counter__btn.--increment
                span.counter__btn__icon +
